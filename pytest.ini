[pytest]
addopts = -p no:warnings -r xX -v --tb=short --color=yes
log_cli=true
log_level=INFO
log_format = %(asctime)s - %(levelname)s - %(filename)s - %(lineno)d - %(message)s
log_date_format = %Y-%m-%d %H:%M:%S

markers =
    home: test home